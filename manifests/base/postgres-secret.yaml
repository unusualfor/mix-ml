# GENERATE REAL VALUES BEFORE APPLY — see README
# Replace placeholder values with: openssl rand -base64 24
#
# TODO: Move to SealedSecrets or ExternalSecrets for non-demo environments.
#       Never commit real credentials. These are placeholders only.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: cocktail-db
  labels:
    app.kubernetes.io/name: cocktail-db
    app.kubernetes.io/part-of: cocktail-platform
type: Opaque
stringData:
  POSTGRESQL_USER: cocktailuser
  POSTGRESQL_PASSWORD: CHANGE-ME-BEFORE-DEPLOY
  POSTGRESQL_ADMIN_PASSWORD: CHANGE-ME-BEFORE-DEPLOY
