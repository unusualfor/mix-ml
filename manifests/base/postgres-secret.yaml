# Placeholder only — actual values are set by scripts/setup-secrets.sh.
# DO NOT commit real credentials here.
# ArgoCD ignoreDifferences is configured to skip /data and /stringData
# so the live secret is never overwritten by these placeholders.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: cocktail-platform
    app.kubernetes.io/managed-by: argocd
type: Opaque
stringData:
  POSTGRESQL_USER: REPLACE_ME
  POSTGRESQL_PASSWORD: REPLACE_ME
  POSTGRESQL_ADMIN_PASSWORD: REPLACE_ME
